global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "aquarium-data-2026"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "localhost:37820"
